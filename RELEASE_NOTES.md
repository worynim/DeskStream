## [v2.5.2] - 2026-04-07
### Added & Fixed (DS_DECK: 브라우저 런처 및 Spotlight 자동화 구현)
- **브라우저 모드(Browser Mode) 신규 구현**: 기기 버튼 클릭 한 번으로 macOS의 Spotlight를 소환하여, 사용자의 **기본 브라우저**로 웹사이트 URL을 직접 실행하는 유니버설 런처 기능을 추가했습니다.
- **Spotlight 자동화 안정성 고도화**:
  - 명령어 입력 시 `KEY_RETURN` 물리 키 코드를 강제 배정하여 `Cmd+Enter` 미작동 버그 완벽 해결.
  - Spotlight 실행 직후 `KEY_BACKSPACE`를 1회 전송하여 기존 검색 잔여 검색어를 깨끗이 삭제하는 무결성 로직 추가.
  - URL 입력 시 기기 내부에 저장된 한영 전환 찌꺼기(`[#CAPS#]`)를 펌웨어 단에서 완벽히 배제하고 순수 문자열만 전송함으로써 타이핑 중 한/영 꼬임 현상을 하드웨어적으로 원천 차단.

## [v2.5.1] - 2026-04-07
### Fixed & Improved (DS_DECK: 코드 무결성 및 시스템 안정성 강화)
- **모드 스위칭 엔진 정상화**: BLE 연결 상태에서 멀티미디어 키와 복합 단축키가 무시되던 치명적인 조건문 오류를 해결했습니다.
- **엔드포인트 보안(Endpoint Security)**: 웹 서버 API 호출 시 유효 범위를 벗어난 인덱스 접근을 차단하는 가드 루틴을 적용하여 시스템 다운 현상을 방지했습니다.
- **비휘발성 메모리(LittleFS) 신뢰성**: 설정 데이터 로딩 중 발생할 수 있는 JSON 파싱 에러에 대한 폴백 로직을 구축하고 파일 클로징 프로세스를 최적화했습니다.
- **빌드 및 환경 최적화**: 중복된 매크로 정의를 정리하고 업로드 핸들러의 파일 객체 생명 주기를 효율화했습니다.

## [v2.5.0] - 2026-04-07
### Improved (DS_DECK: macOS 호환성 극대화 및 단축키 엔진 고도화)
- **macOS 전용 한/영 전환 로직**: `KEY_CAPS_LOCK`의 BLE 한계를 극복하기 위해 macOS 표준 단축키(`Ctrl + Space`)를 이용한 지능형 언어 전환 엔진을 구현했습니다.
- **단축키(Combo) 모드 대폭 확장**: `getHIDFromASCII` 엔진을 8비트 풀 스캔 방식으로 개편하여 F1~F12, 방향키, 엔터, 기호 등 모든 표준 키보드 입력을 완벽하게 지원합니다.
- **웹 대시보드 UI/UX 혁신**: 
    - 특수키 드롭다운 메뉴를 도입하여 복잡한 단축키 구성을 마우스 클릭만으로 설정할 수 있도록 개선했습니다.
    - 현재 기기에 설정된 특수키 상태가 웹페이지에 정확히 표시되도록 동기화 로직을 강화했습니다.
- **햅틱 피드백 강화**: 웹 설정 적용 시 기기에서 즉시 성공 알림음(Beep)을 출력하여 원격 설정의 신뢰성을 높였습니다.

## [v2.4.1] - 2026-04-05
### Improved (Stats_Monitor: 밝기 설정 최적화 및 사용자 정의 수치 적용)
- **밝기 관리 배열화**: 하드코딩된 매직 넘버를 `BRIGHTNESS_LEVELS` 배열로 개선하여 코드 가독성과 유지보수성을 확보했습니다.
- **사용자 요청 밝기 반영**: 4단계 밝기 값을 **1, 50, 100, 240**으로 새롭게 조정하여 더 넓은 범위의 밝기 선택지를 제공합니다.
- **도움말 UI 동기화**: 변경된 밝기 단계가 버튼 도움말(BTN4) 창의 비주얼 가이드에 즉각 반영되도록 로직을 업데이트했습니다.
- **트레이 메뉴 및 브랜딩 강화**: 
    - 시스템 트레이 아이콘의 종료 메뉴 이름을 'Quit'에서 '**Desk Stream Stats Monitor Quit**'으로 변경하여 식별성을 높였습니다.
    - 트레이 아이콘의 모니터 화면 중앙에 '**DS**' 로고를 삽입하여 브랜드 정체성을 강화했습니다.
- **macOS 지능형 아이콘(Template Image) 적용**: macOS 상태 표시줄의 투명도 및 배경화면 밝기에 따라 아이콘 색상이 자동으로 전환(흑/백)되는 '**Template Mode**'를 강제 활성화했습니다. (`pyobjc`를 통한 시스템 레벨 제어 도입)
- **상태 바 시인성 극대화**: 배경을 완전 투명화하고 시스템 표준 템플릿 설계를 적용하여, 다크/라이트 모드 및 다양한 배경화면 환경에서도 최상의 가시성을 보장합니다.
- **크로스 플랫폼(Windows) 지원 고도화**: 
    - 윈도우 환경 실행 시 폰트 깨짐 방지를 위해 `ImageFont` 경로를 동적(OS별)으로 매핑하도록 폰트 로딩 로직을 개선했습니다.
    - 윈도우의 레지스트리를 읽어 작업 표시줄 테마(Dark/Light)를 주기적으로 자동 감지하고, 그에 맞춰 트레이 아이콘 색상(흰색 또는 검은색)을 즉시 교체하여 이질감 없는 UI를 보장합니다.

## [v2.3.0] - 2026-04-05
### Added (Smart_Info_Station: 도움말 페이지 고도화 및 부저 정밀도 개선)
- **인터랙티브 설정 상태 표시**: 5번 도움말 페이지에서 현재의 **화면 회전(Flip) 모드** 명칭과 **OLED 밝기** 단계를 실시간으로 확인할 수 있는 시각적 가이드를 추가했습니다.
- **부저 타이밍 정밀 제어 (Buzzer Sync)**: FreeRTOS 타이머를 도입하여, OLED 통신 등 무거운 작업 중에도 부저음이 설정된 시간(50ms) 동안만 정확하게 울리도록 로직을 혁신했습니다. (버튼별로 소리 길이가 달랐던 현상 해결)

## [v2.2.1] - 2026-04-04
### Improved (Smart_Info_Station: 주말 데이터 업데이트 최적화)
- **주말 데이터 업데이트 전면 차단**: 토요일/일요일 등 시장 휴장일에 불필요하게 서버를 호출하지 않도록 개장 판단 로직을 전면 고도화했습니다.
    - **국내 증시(KOSPI, KOSDAQ 등)**: 분 단위 정밀 체크 추가 (08:30~16:10).
    - **미국 증시 및 환율(USD/KRW)**: 한국 시간 기준 토요일 오전 8시부터 일요일 전체를 차단하여 시스템 부하를 줄였습니다.
- **시장 판단 로직 정교화**: `isDomesticMarketOpen`, `isUsMarketOpen` 함수에 분(Minute) 단위 체크를 도입하여 개폐 시간 경계에서 더 정확하게 동작합니다.

## [v2.2.0] - 2026-04-04
### Major Update (Smart_Info_Station: 강제 업데이트 강화 및 WiFi 초기화 기능 도입)
- **BTN4 강제 업데이트 로직 혁신**: 
    - **장외 시간 갱신 보장**: 시장 개계 여부와 관계없이 BTN4 롱 프레스로 모든 금융 데이터를 즉시 강제 갱신할 수 있도록 개선했습니다. (장외 시간에 0으로 표시되던 현상 해결)
    - **전체 위젯 동시 갱신**: 현재 가시적인 페이지뿐만 아니라 활성화된 모든 페이지(1~3페이지 전체)의 데이터를 배경에서 함께 갱신하여 페이지 전환 시 즉각적인 최신 정보를 보장합니다.
- **WiFi 공장 초기화(Factory Reset) 기능**: 
    - 전원 인가 시 **BTN 1** 버튼을 누르고 있으면 저장된 WiFi 설정(SSID, Password)을 즉시 삭제하는 안전 장치를 마련했습니다. (공유기 변경 시 유용)
    - 리셋 트리거 시 OLED에 "!!! WiFi RESET !!!" 경고 메시지와 함께 특수 비프음을 출력하여 직관적인 피드백을 제공합니다.
- **데이터 엔진 안정성 및 무결성 확보**:
    - `processed_mask` 기반의 통합 업데이트 로직을 도입하여 중복 데이터 수집을 원천 차단했습니다.
    - 특정 상황에서 발생할 수 있었던 루프 인덱스 초과 버그를 수정하여 장기 구동 안정성을 강화했습니다.

## [v2.1.0] - 2026-04-03
### Improved (Stats_Monitor: 데이터 정밀도 고도화 및 범용 그래픽카드 지원)
- **CPU 측정 정확도 혁신**: 0.1초 단편적 측정 방식에서 호출 주기간의 **전체 평균 점유율**을 계산하는 방식으로 개편하여, 작업 관리자와의 오차를 최소화했습니다.
- **범용 Windows GPU 모니터링**: `GPUtil`(NVIDIA 전용)의 한계를 넘어 Windows 성능 카운터를 직접 인터페이스함으로써 **인텔 내장 그래픽(iGPU), AMD, NVIDIA** 등 모든 제조사의 GPU 부하량을 성공적으로 수집합니다.
- **네트워크 표시 단위 표준화**: 기존 Byte 단위(MB/s, KB/s)를 전 세계 네트워크 표준인 Bit 단위(**Mbps, kbps**)로 변경하여 직관적인 속도 시인성을 확보했습니다.
- **사용자 가이드 및 배포 편의성**: `README.md` 내 Python 클라이언트 설치 및 실행 가이드를 보강하고, `Pillow` 등 필수 라이브러리 의존성 문제를 해결했습니다.

## [v2.0.0] - 2026-04-02
### Major Update (Stats_Monitor: 하드웨어 컨트롤 레이어 및 비주얼 가이드 도입)
- **하드웨어 제어 기능 통합**: 4개의 버튼과 부저(Pin 7)를 활용한 입출력 시스템을 구축하여 데스크 가젯으로서의 완성도를 높였습니다.
    - **BTN 1**: 180도 화면 회전(Flip) 및 물리 배치에 따른 데이터 출력 순서(**4-3-2-1**) 자동 동기화.
    - **BTN 2**: 4단계 밝기(25%/50%/75%/100%) 조절 기능.
    - **BTN 4**: 대화형 도움말 및 실시간 상태 모니터링 페이지 토글.
- **인터랙티브 비주얼 가이드**: 도움말 모드에서 현재의 플립 각도와 밝기 단계를 사각형 심볼(**◽️◼️**)로 실시간 시각화하여 직관적인 피드백을 제공합니다.
- **렌더링 엔진 정밀 최적화**: 
    - **U8G2 공유 버퍼 결함 해결**: 4개 화면이 단일 메모리를 공유하면서 발생하던 렌더링 오버라이트(Overlap) 버그를 구조적으로 해결하여 각 패널의 데이터 독립성을 보장합니다.
    - **Flicker-Free 상태 관리**: 상태(연결, 회전, 도움말) 변화 시에만 화면을 갱신하도록 설계하여 불필요한 깜빡임을 완벽히 제거했습니다.
- **환경 설정 영속성 (NVS)**: 조작한 회전 및 밝기 설정이 재부팅 후에도 유지되도록 비휘발성 저장 로직을 적용했습니다.
- **시스템 안정성 강화**: BLE 통신 변수(`volatile`) 보호 및 비차단 부저 시스템 적용으로 멀티태스킹 환경에서의 데이터 처리 신뢰성을 확보했습니다.

## [v1.9.0] - 2026-04-02
### Major Update (Smart_Info_Station: 비차단 엔진 고도화 및 부팅 UX 혁신)
- **비차단(Non-blocking) 부저 시스템 구축**: `delay()` 기반의 블로킹 부저를 `millis()` 예약 방식으로 전면 개편했습니다. 이제 부저가 울리는 중에도 웹 서버 응답, 시계 갱신, 버튼 조작이 멈춤 없이 매끄럽게 동작합니다.
- **2단계 우선순위 초기 로딩 (Priority Load)**: 부팅 시 현재 페이지의 데이터를 최우선으로 수집하며 OLED에 "Updating..." 상태를 즉시 표시합니다. 나머지 백그라운드 페이지 데이터는 이후에 조용히 로드하여, 초기 구동 시 화면이 오래 비어있던 UX 문제를 해결했습니다.
- **청각적 부팅 피드백 (Startup Melody)**: 시스템 준비 완료 시 경쾌한 4음 상승 멜로디(도미솔도)를 재생합니다. 마그네틱 부저의 특성을 고려하여 음 재생 직후 잔류 전류를 물리적으로 차단하는 보호 로직을 적용했습니다.
- **아키텍처 확장성 강화 (Macro-driven Design)**: 데이터 페이지 수 및 슬롯 구성을 `MAX_DATA_PAGE` 매크로로 단일화했습니다. 이제 코드 수정 없이 상수 변경만으로 화면 구성을 자유롭게 확장할 수 있습니다.
- **안정성 및 품질 혁신**:
    - **레이스 컨디션 해결**: `force_update` 플래그 조작 시 Critical Section 보호를 적용하여 멀티태스크 환경에서의 충돌을 원천 차단했습니다.
    - **웹 설정 UI 최적화**: 페이지별 화면 배치 설정 코드를 루프 기반으로 리팩토링하여 코드 양을 70% 절감하고 로딩 속도를 개선했습니다.
    - **애니메이션 정밀화**: 모든 위젯이 공유하던 정적 카운터를 제거하고 개별 상태 기반의 수집 메시지 표시 로직으로 정교화했습니다.

## [v1.8.0] - 2026-04-02
### Major Update (Sound_Visualizer_4_OLEDs: 오실로스코프 모드 및 대화형 도움말 도입)
- **실시간 파형 시각화 (Oscilloscope Mode)**: 주파수 분석 외에 시간 영역의 파형을 직접 관찰할 수 있는 기능을 추가했습니다. 자동 스케일링을 통해 어떤 소리 크기에서도 역동적인 파형을 볼 수 있습니다.
- **사용자 가이드(Help) 인터페이스**: 2번 버튼(GPIO 4)을 길게 누르면 현재 버튼들의 기능을 OLED에 즉시 표시하는 도움말 화면을 구축했습니다.
- **시각화 모드 즉시 전환**: 4번 버튼(GPIO 9)의 숏프레스 동작을 모드 전환으로 할당하여 스펙트럼 막대와 오실로스코프 파형을 자유롭게 오갈 수 있습니다. (롱프레스 캘리브레이션은 유지)
- **고도화된 버튼 응답 시스템**: 모든 버튼에 숏/롱 프레스를 구분하는 고성능 비차단 스캔 로직을 적용하여 UI 인터랙션의 질을 높였습니다.

## [v1.7.0] - 2026-04-02
### Improved (Sound_Visualizer_4_OLEDs: 비차단 오디오 및 렌더링 로직 정밀화)
- **비차단(Non-blocking) 부저 시스템 이식**: `GPS_DASH`에서 검증된 `millis()` 기반 비차단 톤 제어 로직을 `Sound_Visualizer`에 이식했습니다. 이제 비프음 출력 중에도 ADC DMA 샘플링이 멈추지 않아 파형 끊김 현상이 완전히 해결되었습니다.
- **지능형 AGC(Auto Gain Control) 고도화**: AGC의 기준 스케일을 기존 'Raw FFT 데이터'에서 '최종 매핑/노이즈 차감 후의 밴드 진폭'으로 변경했습니다. 이로써 로그(Log) 및 거듭제곱(Power) 매핑 모드에서도 실제 시각적 높이에 최적화된 감도가 유지됩니다.
- **코드 무결성 및 빌드 안정화**:
    - **컴파일 오류 해결**: Arduino IDE의 자동 전방선언 특성을 고려하여 `ToneNote` 구조체 정의 위치를 최상단으로 조정했습니다.
    - **상수 정규화**: 하위 레벨의 매직 넘버들을 `config.h` 상수로 통합하고, 잘못 기재되어 있던 설정 주석(FPS, 샘플링 주파수 등)을 실제 값에 맞게 전면 교정했습니다.
    - **워치독 안전성**: 캘리브레이션 대기 구간의 블로킹 `delay()`를 `vTaskDelay`로 대체하여 시스템 안정성을 강화했습니다.
- **프로젝트 사양 교정**: README 내 ESP32-C3의 FPU 유무 정보를 "FPU 없음"으로 바로잡아(정수형 FFT 사용 배경 명시) 기술적 정확도를 확보했습니다.

## [v1.6.0] - 2026-04-02
### Major Update (GPS_DASH: 비차단 엔진 및 전력 최적화 시스템 도입)
- **비차단 오디오 파이프라인**: GPS 데이터 수신 도중 부저 멜로디가 시스템을 멈추게(Blocking) 하던 문제를 해결하기 위해 `millis()` 기반 비차단 톤 시스템을 구축했습니다. 이제 알림음 재생 중에도 GPS 데이터 누락 없이 실시간 갱신이 가능합니다.
- **지능형 WiFi 전력 관리**: 배터리 소모 및 발열을 줄이기 위해 부팅 후 5분간만 OTA용 WiFi AP를 유지하고 이후 자동 종료하는 로직을 도입했습니다. (접속 중인 클라이언트가 있으면 종료 대기 및 연장)
- **코드 리뷰 기반 품질 혁신 (Total 12+ items)**:
    - **Flash 수명 보호**: NVS(Preferences) 저장 방식을 '변경 즉시'에서 '3초 대기 후 통합 저장'으로 개선.
    - **렌더링 최적화**: 10Hz로 반복되던 도움말(INFO) 화면에 Dirty Flag를 적용하여 CPU 점유율 대폭 절감.
    - **글로벌 대응**: 위도/경도 표시 시 `N/S`, `E/W`를 좌표 부호에 따라 자동 판별하도록 개선.
    - **중복 호출 및 데드 코드 정리**: 불필요한 초기화 시퀀스 통합 및 미사용 버퍼/함수 제거를 통해 RAM 및 실행 효율 최적화.
- **하드웨어 진단 모드**: `DEBUG_GPS` 플래그를 통해 시리얼 모니터로 원시 NMEA 데이터를 실시간 모니터링할 수 있는 디버그 인프라를 마련했습니다.

## [v1.5.6] - 2026-04-01
### Improved (OLED_Wide_test: 버튼 입력 반응성 비약적 향상)
- **실시간 버튼 핸들링 강화**: 화면 그리기(Rendering) 도중이나 프레임 대기(Frame Pacing) 구간에서도 버튼 입력을 지속적으로 스캔하여, 어느 시점에서도 즉각적인 화면 플립과 비프음 피드백이 가능하도록 로직을 재설계했습니다.

## [v1.5.5] - 2026-04-01
### Added (OLED_Wide_test: 180도 화면 플립 및 햅틱 피드백 도입)
- **180도 화면 회전 (Flip) 기능**: 1번 버튼(GPIO 1)을 통해 전체 디스플레이를 180도 뒤집는 기능을 구현했습니다.
- **다이나믹 디스플레이 스왑**: 화면이 뒤집힐 때 가상 캔버스의 구역 슬라이싱 로직을 역순(`1-2-3-4 -> 4-3-2-1`)으로 자동 전환하여, 가로 방향 애니메이션(Ticker 등)의 연속성을 완벽하게 유지합니다.
- **부저 피드백 및 NVS 저장**: 버튼 조작 시 3000Hz 비프음을 추가하고, 플립 상태를 비휘발성 메모리(Preferences)에 저장하여 재부팅 시 설정을 복원합니다.
- **성능 최적화**: I2C 클럭을 800kHz로 상향 조정하고, 지연(Pause) 시간 중에도 버튼 반응성을 유지하도록 `checkButtons()` 핸들러를 `smartDelay`에 통합했습니다.

## [v1.5.4] - 2026-04-01
### Added (Sound_Visualizer_4_OLEDs: 햅틱 부저 및 180도 스마트 회전 시스템 도입)
- **오디오 피드백 적용**: 캘리브레이션 트리거 시 짧은/긴 진입음에 대한 직관적인 펄스 제어(`beep()`) 적용.
- **180도 스마트 플립 기능**: 1번 핀 버튼 클릭 시 OLED 데이터 하드웨어 출력을 180도 회전시킴과 동시에, 가상 캔버스의 구역 슬라이싱 로직을 역순(`1-2-3-4 -> 4-3-2-1`)으로 자동 스왑하여 파형의 연속적 흐름 유지.
- **NVS 저장소 구축**: 변경된 플립 상태를 비휘발성 메모리(Preferences)에 실시간 캐싱하여 재부팅 시에도 표시 상태 완벽 보존.

## [v1.5.3] - 2026-04-01
### Added (GPS_DASH: 버튼 피드백 부저 추가)
- **버튼 조작음(Buzzer) 피드백 추가**: 7번 핀에 패시브 부저 펄스 제어(`beep()`) 로직을 추가하여, 짧은 클릭(Short)과 길게 누르기(Long) 조작 시 직관적인 청각적 피드백 제공.
- **프로젝트 간 일관성 유지**: `Smart_Info_Station`의 `beep()` 함수 구조를 `GPS_DASH`에 동일하게 복제하여 피드백 스니펫의 범용성 확보.

## [v1.5.2] - 2026-04-01
### Added & Fixed (Smart_Info_Station: 버튼 피드백 및 UI 매핑 고도화)
- **버튼 조작음(Buzzer) 피드백 추가**: 7번 핀에 패시브 부저 펄스 제어(`tone()`) 로직을 추가하여, 짧은 클릭(Short)과 길게 누르기(Long) 조작 시 청각적 피드백 제공.
- **직관적인 도움말 매핑 고정**: Flip Mode 설정으로 인해 화면이 교차(Swap) 출력되더라도, '버튼 설정 가이드 페이지'는 사용자의 물리적인 버튼 위치와 일치하도록 고정 매핑되게 예외 처리 적용.
- **Flip 전환 잔상 완전 제거**: 화면 회전 버튼 누름 즉시 4개의 OLED 하드웨어 버퍼를 일제히 초기화(`clearBuffer`)하고 전체 화면을 강제 재렌더링하여 발생하던 디스플레이 잔상(Ghosting) 문제 해결.

## [v1.5.1] - 2026-03-28
### Improved (Stats_Monitor: 부팅 시 OLED 대기 화면 안내 시스템 도입)
- **클라이언트 실행 유도 UX 개선**: 보드 전원 투입 직후(또는 연결 대기 중) OLED가 비어 있지 않도록 "PLEASE RUN PC CLIENT APP" 안내 메시지가 4개 OLED에 걸쳐 즉시 표시되도록 개선.
- **초기 렌더링 응답성 강화**: 펌웨어 내 상태 머신(`lastState = -1`)을 적용하여 부팅 및 재접속 시의 시각적 피드백 속도 최적화.

## [v1.5.0] - 2026-03-28
### Improved (Smart_Info_Station: 성능 최적화 및 스마트 루핑 도입)
- **화면 깜빡임 제로(Flicker-Free)**: 이중 버퍼 정제 로직 최적화를 통해 페이지 전환 시의 깜빡임을 제거하고 부드러운 UI 전환 구현.
- **스마트 자동 루핑 시스템**: 데이터 수집 중일 때 페이지 전환을 자동 유예하여, 수시로 변하는 정보의 수집 완료를 보장하는 지능형 루핑 도입.
- **위젯별 독립 수집 주기 관리**: 글로벌 타이머의 한계를 극복하고 각 서비스(금융, 날씨 등)별 독립적인 갱신 주기를 철저히 준수하도록 엔진 개선.
- **사용자 편의성 강화**: 폰트 중앙 관리 매크로(`MAIN_NUM_FONT`) 도입 및 기본 밝기(25%) 최적화를 통한 번인 방지.

## [v1.4.0] - 2026-03-28
### Added (Smart_Info_Station: 4버튼 내비게이션 및 사용자 인터페이스 확장)
- **4버튼 독립 제어 시스템 구축**: `GPS_DASH`의 버튼 아키텍처를 이식하여 4개 버튼에 화면 회전, 밝기 조절, 페이지 전환, 데이터 새로고침 기능을 독립적으로 할당.
  - **BTN 1**: 짧게(Short) - **화면 회전(Flip)** / 길게(Long) - (Reserved)
  - **BTN 2**: 짧게(Short) - **밝기(Contrast)** 4단계 조절 / 길게(Long) - (Reserved)
  - **BTN 3**: 짧게(Short) - (Reserved) / 길게(Long) - **자동 루핑(Auto Loop)** ON/OFF
  - **BTN 4**: 짧게(Short) - **페이지 전환(Next)** / 길게(Long) - **데이터 강제 새로고침**
- **인터랙티브 도움말 시스템**: 버튼별 현재 기능을 OLED에서 즉시 확인할 수 있는 도움말 페이지(**Page 5**) 및 IP 정보(**Page 4**) 배치.
- **사용자 설정 영속성**: 회전, 밝기, 루핑 설정을 `Preferences`에 저장하여 재부팅 후에도 개인화된 설정 유지.
- **웹 대시보드 확장**: 자동 루핑 설정을 웹 설정 UI 카드 내부로 통합하여 실시간 원격 제어 지원.

---

## [v1.3.0] - 2026-03-28
### Refactored (Smart_Info_Station 프로젝트 대규모 리팩토링)
- **전면 모듈화 아키텍처 도입**: 단일 파일 구조에서 기능별(Display, Layout, Data, Web) 독립 모듈 시스템으로 전환하여 유지보수성 극대화.
- **데이터 파싱 신뢰성 확보**: 유실되었던 금융, 유튜브, 날씨 API 파싱 엔진의 정교한 복구 및 안정성 강화.
- **빌드 시스템 최적화**: C++ 표준 헤더/소스 분리 기법 적용을 통한 링커 에러 근절.

---

## [v1.2.0] - 2026-03-27
### Enhanced (Stats_Monitor 프로젝트 고도화)
- **실시간 네트워크 통합 모니터링**: 의미가 고정적인 전력 수치 대신, 생동감 넘치는 다운로드/업로드 속도 실시간 측정 로직 도입.
- **OLED 렌더링 물리 엔진 최적화**: 
  - **Shadow Buffer** 기반의 Dirty Checking 기법을 수동으로 구현하여 I2C 전송 대역폭 효율 극대화.
  - ESP32-C3 하드웨어 레지스터 직접 제어(Register-direct)를 통한 소프트웨어 I2C 성능 획기적 개선.
  - I2C 버스 클럭 **800kHz** 상향 조정.
- **가독성 기반 UI/UX 리뉴얼**: 
  - 7-Segment 스타일 및 `profont15` 적용으로 세련된 대시보드 시각화 구현.
  - 모든 상태 지표를 '라벨+수치+게이지 바' 레이아웃으로 통일하여 통합된 디자인 경험 제공.
- **시스템 모니터 트레이 아이콘 최적화**: macOS 투명 상태 바에 최적화된 화이트 실루엣 아이콘 적용.
- **코드 무결성 강화**: 모든 프로젝트 소스 코드 및 최적화 기법에 대한 상세 기술 주석 추가 완료.

---

## [v1.1.0] - 2026-03-26
### Added (Stats_Monitor 프로젝트 강화)
- **macOS(M2/M3) 정밀 지표 매칭**: `psutil` 및 시스템 파티션 분석을 통해 상용 앱(`mac stats`)과 동일한 수준의 RAM/Disk 수치 동기화 성공.
- **Apple Silicon GPU 실시간 모니터링**: `ioreg` 성능 지표 파싱을 통한 GPU 가동률 추출 로직 구현.
- **실시간 센서 스트림 파서**: 백그라운드에서 `powermetrics` 데이터를 실시간으로 낚아채는(Hooking) 스레드 파서 클래스(`PowerMetricsReader`) 도입 (1Hz 갱신).
- **BLE 통신 인프라 안정화**: UART 서비스 기반의 비동기 전송 프로토콜 구축 및 자동 재접속(Auto-Reconnect) 로직 강화.
- **UI/UX 고도화**: 대형 폰트(`maniac_tr`) 및 슬림 프로그레스 바를 적용한 GPS_DASH 스타일의 세련된 대시보드 레이아웃 적용.
- **디버깅 가시화**: 펌웨어 및 파이썬 클라이언트 전반에 실시간 데이터 수신 상태 로그(Serial/Terminal) 추가.
- **계획 수립**: `PLAN.md`를 통한 차기 개발 옵션 구체화.

---

## [v1.0.0] - 2026-03-26
### Added
- DeskStream 프로젝트 초기 구성 완료.
- 4가지 주요 프로그램 통합 관리:
  - `Smart_Info_Station`: 스마트 정보 스테이션 (시계, 날씨, 증시 등)
  - `GPS_DASH`: GPS 기반 드라이빙 대시보드
  - `Sound Visualizer 4 OLEDs`: 마이크 입력 기반 고성능 오디오 비주얼라이저
  - `OLED_Wide_test`: 4개 OLED 통합 캔버스 제어 테스트
  - `Stats_Monitor`: 시스템 상태 모니터 (CPU, GPU, RAM, SSD 등)
- 하드웨어 명세서(`hardware.md`) 및 회로도 연결 정보 정리.
- 루트 디렉토리 `README.md` 생성 및 프로젝트 가이드라인 구축.
